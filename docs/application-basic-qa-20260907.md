# 网申基础功能回归（2026-09-07）

## 范围和权限

- 主目录本地开发服务（localhost:3000），当前用户账号。
- 用户明确允许同步所选简历、创建一条“功能测试”投递记录。
- 未修改或删除既有投递记录，未保存或提交任何招聘官网申请。
- 未运行数据库迁移、修改密钥、部署或推送代码。

## 修复

1. 选择默认简历被当成资料内容修改，导致“从简历补充空缺”要求先保存。
   - 分开比较已保存的资料和默认简历；仅真正的资料内容修改阻止同步。
   - 同步成功更新保存快照；同步期间禁止修改表单；保存响应不会清掉后续编辑。
2. 投递详情两个日期浮层固定在左侧，在窄桌面窗口出现边缘裁切。
   - 改为优先向下展开，允许上下碰撞避让；高度受可用空间约束，内部可滚动。

## 当前账号真实浏览器结果

| 检查 | 结果 |
| --- | --- |
| 选择原先选中的简历后直接补充 | 通过，无须先保存空资料 |
| 刷新后默认简历、同步内容保留 | 通过 |
| 本轮新同步资料读取解密 | 通过；不代表历史损坏密文已恢复 |
| 点击生日输入框、中文日历 | 通过，未改变生日 |
| 新建投递记录 | 通过 |
| 测试记录改为已投递、保存备注、重新打开详情 | 通过，进展记录显示状态变更 |
| 待完成筛选再次点击取消 | 通过 |
| 日期浮层修复后视觉核对 | 通过，位于输入框下方，中文且可滚动 |
| 插件侧栏 | 阻塞：浏览器显示 ERR_FILE_NOT_FOUND，未进入插件功能 |

保留的测试数据：公司“功能测试-20260907”，职位“网申基础回归（非真实投递）”。
状态“已投递”仅用来测试本产品的状态保存，未向招聘网站提交；备注已说明此用途。

## 自动化验证

| 命令 | 结果与覆盖 |
| --- | --- |
| `pnpm exec vitest run --config scripts/vitest.application-ui.config.ts` | 9 项：选择后直接同步、手动修改保护、恢复原值、失败重试、并发保护、保存期间新编辑 |
| `pnpm run test:application-autofill` | 11 项通过 |
| `pnpm exec vitest run --config scripts/vitest.extension-server.config.ts` | 17 项通过 |
| `pnpm --dir extension run test` | 80 项通过；不代替真实招聘网站验收 |
| `pnpm exec tsc --noEmit --incremental false` | 通过 |
| `node scripts/test-application-profile-ui.mjs` | 合成接口浏览器回归通过：同步、保存失败/重试、中文日期、下拉位置、手机布局 |
| `node scripts/test-application-ui-guards.mjs` | 合成接口浏览器回归通过：关闭保护、取消筛选、防重复保存；两日期浮层在 1280/892/390px 均不越界且位于输入框上下方 |
| `pnpm --dir extension run build`（进程环境 `WXT_API_BASE_URL=http://localhost:3000`） | 本地测试插件构建通过，输出 `extension/.output/chrome-mv3` |

单元/组件测试合计 117 项。两个浏览器脚本拦截接口、使用合成数据，不写真实数据库，不能证明生产鉴权及后端端到端行为。
图片产物：`test-artifacts/application-profile-basic-qa/desktop.png`、`mobile.png`。
浏览器测试等待保存按钮恢复可用后再操作；曾因未等待保存结束点击禁用控件超时，已修正测试同步条件并重跑通过。

## 尚未验收

- 当前已安装插件缺少可读取的侧栏文件。旧 worktree 移动/删除是可能原因，但本次尚未核实安装来源路径。
- 已重新生成主目录的本地测试构建，需用户在 Chrome 中重新加载该目录后，继续验证连接、资料读取和真实填表。
- 腾讯/淘天本轮未执行填表；插件埋点真实上报链路未复验。
- 本轮没有进行 Next 生产构建，避免干扰用户运行中的开发服务；未部署。

结论：网申资料和投递管理本轮基础路径通过，发现的两处前端问题已修复；插件真实端到端回归因安装文件缺失仍未完成，不能据此宣布整体发布验收通过。
