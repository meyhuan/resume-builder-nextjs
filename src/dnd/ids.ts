/**
 * DndId prefixes used to build unique identifiers for dnd-kit.
 * Keep a single export per file as per project conventions.
 */
export const DndIds = {
  SECTION_SORT_ID_PREFIX: 'sec-',
  SECTION_DROP_ID_PREFIX: 'section-',
} as const
