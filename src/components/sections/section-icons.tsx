import type { ReactElement } from 'react';
import { Briefcase, GraduationCap, Code, Award, User, Target, Star, BookOpen } from 'lucide-react';

/**
 * Section icons for resume sections.
 */

export function IconBriefcase(): ReactElement {
  return <Briefcase size={18} />;
}

export function IconGraduationCap(): ReactElement {
  return <GraduationCap size={18} />;
}

export function IconCode(): ReactElement {
  return <Code size={18} />;
}

export function IconAward(): ReactElement {
  return <Award size={18} />;
}

export function IconUser(): ReactElement {
  return <User size={18} />;
}

export function IconTarget(): ReactElement {
  return <Target size={18} />;
}

export function IconStar(): ReactElement {
  return <Star size={18} />;
}

export function IconBook(): ReactElement {
  return <BookOpen size={18} />;
}

