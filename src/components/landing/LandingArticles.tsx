'use client';

import type { ReactElement } from 'react';

interface ArticlesSectionProps {
  readonly id?: string;
}

export const LandingArticles = ({ id }: ArticlesSectionProps): ReactElement => {
  void id;
  return <></>;
};
